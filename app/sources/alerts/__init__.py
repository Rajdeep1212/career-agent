"""Job-alert email ingestion (LinkedIn, Naukri, Indeed): IMAP on a dedicated account, or .eml files.

Only the email is read. The job links it contains are stored and shown, but the
pages behind them are never fetched (CLAUDE.md hard constraint).
"""
